@model IEnumerable<MVC_kawiarnia.Models.JumbotronText>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.JumbotronIdText1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.JumbotronIdText2)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.JumbotronIdText1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.JumbotronIdText2)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.JumbotronTextId }) |
            @Html.ActionLink("Details", "Details", new { id=item.JumbotronTextId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.JumbotronTextId })
        </td>
    </tr>
}

</table>
