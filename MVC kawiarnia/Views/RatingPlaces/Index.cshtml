@model IEnumerable<MVC_kawiarnia.Models.RatingPlace>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RatingPlaceInt)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RatingPlaceInt)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RatingPlaceId }) |
            @Html.ActionLink("Details", "Details", new { id=item.RatingPlaceId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RatingPlaceId })
        </td>
    </tr>
}

</table>
