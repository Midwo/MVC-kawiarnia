@model IEnumerable<MVC_kawiarnia.Models.RatingSummary>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RatingPlaceText)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RatingPlaceText)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RatingSummaryId }) |
            @Html.ActionLink("Details", "Details", new { id=item.RatingSummaryId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RatingSummaryId })
        </td>
    </tr>
}

</table>
