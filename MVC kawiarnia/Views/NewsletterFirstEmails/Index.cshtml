@model IEnumerable<MVC_kawiarnia.Models.NewsletterFirstEmail>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Body)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Signature)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Body)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Signature)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.NewsletterFirstEmailId }) |
            @Html.ActionLink("Details", "Details", new { id=item.NewsletterFirstEmailId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.NewsletterFirstEmailId })
        </td>
    </tr>
}

</table>
