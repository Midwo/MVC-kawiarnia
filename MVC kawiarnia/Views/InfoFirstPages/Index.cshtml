@model IEnumerable<MVC_kawiarnia.Models.InfoFirstPage>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UnderString)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Files)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UnderTitle)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.Raw(item.Title)
        </td>
        <td>
            @Html.Raw(item.UnderString)
        </td>
        <td>
        <div class="img_small">  <img src="@Html.DisplayFor(modelItem => item.Files)" alt="sample4"> </div>
        </td>
        <td>
            @Html.Raw(item.UnderTitle)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.InfoFirstPageId }) 
        </td>
    </tr>
}

</table>
